# Models

This directory is reserved for storing trained machine learning models.

The project currently implements the following models:
- Logistic Regression
- Random Forest
- Support Vector Machine

Trained model files (e.g., .pkl) are not included in this repository.  
You can save models here after training for future use.
